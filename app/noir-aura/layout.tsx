import "./noir.css";

export const metadata = {
  title: "NOIR AURA Aoyama",
  description: "Cut & Color that feels like luxury.",
};

export default function NoirAuraLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}